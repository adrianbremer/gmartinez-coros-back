'use strict';

/**
 * event-program-song router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::event-program-song.event-program-song');
