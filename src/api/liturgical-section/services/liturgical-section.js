'use strict';

/**
 * liturgical-section service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::liturgical-section.liturgical-section');
