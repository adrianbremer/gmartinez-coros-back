'use strict';

/**
 * liturgical-section router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::liturgical-section.liturgical-section');
